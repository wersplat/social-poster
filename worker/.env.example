# Supabase
SUPABASE_URL=
SUPABASE_SERVICE_KEY=

# X / Twitter — app-level credentials
X_API_KEY=
X_API_SECRET=

# X / Twitter — fallback account (optional if all leagues have per-league creds)
X_ACCESS_TOKEN=
X_ACCESS_SECRET=
