# Supabase (service role — keep secret; never commit .env)
SUPABASE_URL=
SUPABASE_SERVICE_KEY=

# X app credentials
X_API_KEY=
X_API_SECRET=

# Default X user tokens (fallback when per-league tokens are not in webhook_config)
X_ACCESS_TOKEN=
X_ACCESS_SECRET=
